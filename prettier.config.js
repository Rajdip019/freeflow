// prettier.config.js
module.exports = {
  plugins: [require("prettier-plugin-tailwindcss")],
  semi: true, // use semicolons
  singleQuote: false, // if true, use single quotes instead of double quotes
  tabWidth: 2, // number of spaces per indentation level
};
